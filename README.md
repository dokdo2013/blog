# 👋 Haenu World

```typescript
function greet(name: string, languages: string[]): void {
  console.log(`Hello, ${name}! I see that you like ${languages.join(' and ')}!`);
}

const name: string = 'Hyeon Woo Jo';
const languages: string[] = ['JavaScript', 'Python'];

greet(name, languages);
```
